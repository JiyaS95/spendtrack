//knows SQL, talks to database
package com.spendtrack.dao;
public class ExpenseDAO {
    /*public List<Expense> getAllExpenses() {
        List <Expense> expenses = new ArrayList<>();
        
        String sql = "SELECT * FROM expenses"; // a sentence for the database

        try {
            Connection conn = DB.getConnection();
            Statement stmt = conn.createStatement();
            //take sentence, make into SQL text format
            ResultSet rs = stmt.executeQuery(sql);//special type to hold many rows, many columns, a table of results
            //stmt = type Statement, messenger to talk to database
            
        }
        catch (Exception e) {}
        
    return expenses;
    }
    */
    
}